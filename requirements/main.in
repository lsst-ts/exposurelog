# Editable runtime dependencies (equivalent to install_requires)
# Add direct runtime dependencies here, as well as implicit dependencies
# with constrained versions.
#
# After editing, update requirements/main.txt by running:
#     make update-deps

# Resolve a dependency issue by forcing a version of packaging
packaging>=22.0
alembic~=1.9
astropy~=5.2
asyncpg~=0.27
fastapi<1
importlib_metadata~=6.0
sqlalchemy~=2.0
structlog~=22.3
uvicorn~=0.20
# Use explicit versions because otherwise pip installs version 24, which is too old.
# Once DM fixes the problem you can just use the lsst-daf-butler[postgres] line,
# hopefullly with a proper release version, and delete the others.
lsst-daf-butler[postgres]==26.0.0a20230100
lsst-resources==26.0.0a20230100
lsst-sphgeom==26.0.0a20230100
lsst-utils==26.0.0a20230500  # 0100 is not available

